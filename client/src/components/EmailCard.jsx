import { MailOpen, Star } from 'lucide-react';
import PriorityBadge from './PriorityBadge.jsx';
export default function EmailCard({ email, selected, onSelect }) { return <button className={`pp-email-card${selected ? ' is-selected' : ''}${email.unread ? ' is-unread' : ''}`} onClick={() => onSelect(email)}><span className="pp-avatar">{email.sender.split(' ').map((x) => x[0]).join('').slice(0, 2)}</span><span className="pp-email-copy"><strong>{email.sender}</strong><b>{email.subject}</b><small>{email.preview}</small></span><span className="pp-email-meta"><time>{email.time}</time><PriorityBadge priority={email.priority} /></span><span className="pp-email-actions">{email.important && <Star size={15} fill="currentColor" />} {email.unread ? <i /> : <MailOpen size={14} />}</span></button>; }
